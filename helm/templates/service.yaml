apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.env }}-{{ .Values.appName }}-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.env }}-{{ .Values.appName }}-app
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
